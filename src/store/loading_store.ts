import { writable } from 'svelte/store';

export const discovery_loading_store = writable(true);
export const search_loading_store = writable(true);
export const bookmark_loading_store = writable(true);
